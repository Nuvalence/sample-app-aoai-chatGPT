# ssh into the vm and run the command below after each deployment/restart

curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && apt-get install -y nodejs; cd frontend; npm install; npm run build;cd ..
